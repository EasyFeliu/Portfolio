/**
 * Contrato común entre `apps/web` (frontend) y `apps/api` (backend).
 * Cambiar algo aquí obliga a que ambos lados se actualicen a la vez.
 */
export * from "./contact";
export * from "./github";
export * from "./types";
